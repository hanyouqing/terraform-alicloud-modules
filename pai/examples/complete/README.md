# PAI complete example

Workspace plus optional dataset, model, and online service. Replace OSS URIs and tune `service_config` before apply.

DashScope/Bailian LLM API keys stay console/out-of-band; this example covers custom PAI training/serving infra.

```bash
terraform init
terraform plan
```
