# Disk Complete Example

Creates disks with optional ECS attachments and automatic snapshot policies.

## Usage

```bash
terraform init
terraform plan -var-file=terraform.tfvars
terraform apply -var-file=terraform.tfvars
```
