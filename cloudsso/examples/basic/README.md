# CloudSSO Basic Example

Creates a user, group, and membership against an existing CloudSSO directory.

## Usage

```bash
terraform init
terraform plan -var='directory_id=d-xxxxxxxxxx'
terraform apply -var='directory_id=d-xxxxxxxxxx'
```
