# CR Basic Example

Personal Edition namespace and private repository (default path for CI image pushes).

## Usage

```bash
terraform init
terraform plan
```

Set a registry password in the Container Registry console before `docker login`.
