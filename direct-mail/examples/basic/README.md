# Direct Mail Basic Example

Registers one domain and one batch sender address. Complete SPF/DKIM DNS verification after apply.

## Usage

```bash
terraform init
terraform plan -var='domain_name=mail.example.com'
```
