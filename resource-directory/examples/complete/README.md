# Resource Directory Complete Example

Demonstrates directory (optional), nested folders, member account, control policy attachment, and optional CloudSSO delegated administrator.

## Caution

Member account creation is difficult to reverse. Keep `create_resource_directory` and `enable_delegated_admin` false unless you intend to change master-account organization settings.

## Usage

```bash
terraform init
terraform plan
terraform apply
```
