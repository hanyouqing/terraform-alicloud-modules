# CMS Complete Example

Contacts, contact group, multi-severity metric alarm, site monitor, and monitor group.

## Usage

```bash
terraform init
terraform apply \
  -var='instance_id=i-xxxxx' \
  -var='contact_mail=ops@example.com'
```
