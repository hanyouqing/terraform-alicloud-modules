# Express Connect Basic Example

Attaches a single VBR to an existing physical connection (ordered offline / via console).

## Usage

```bash
terraform init
terraform plan \
  -var='physical_connection_id=pc-xxxxxxxx'
```
