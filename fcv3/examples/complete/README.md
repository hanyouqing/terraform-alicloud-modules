# Function Compute 3.0 complete example

Production-oriented: VPC config, SLS log_config, optional triggers and custom domains.

Useful for LLM proxy glue / async jobs — pair with PAI and CR.

```bash
terraform init
terraform plan
```
