# EMR complete example

Production-oriented EMR v2: `deletion_protection=true`, PayAsYouGo, encrypted node disks, private vSwitches.

EMR is costly — destroy idle clusters promptly.

```bash
terraform init
terraform plan
```
