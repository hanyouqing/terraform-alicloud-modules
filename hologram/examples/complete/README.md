# Hologram complete example

Production-oriented Hologres: `enable_ssl=true`, VPC endpoints, sized CPU/storage. Override `zone_id`, `vpc_id`, and `vswitch_id` before apply.

```bash
terraform init
terraform plan
```
