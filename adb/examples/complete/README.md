# AnalyticDB complete example

Production-oriented lake cluster: disk encryption, SSL, VPC `security_ips`, optional lake account.

Pass `clusters` / `accounts` via tfvars (account map is not marked wholly sensitive).

```bash
terraform init
terraform plan
```
