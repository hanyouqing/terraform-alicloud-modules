# Cloud Config Basic Example

Single-account configuration recorder plus one managed rule.

## Usage

```bash
terraform init
terraform plan
```

Provider region defaults to `cn-shanghai` (Cloud Config home region).
