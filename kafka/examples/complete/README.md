# Kafka complete example

Enterprise production-oriented Kafka: multi-zone `vswitch_ids`, optional `kms_key_id`, SASL users, topics + consumer groups.

Pass `sasl_users` via tfvars with passwords (map is not marked wholly sensitive).

```bash
terraform init
terraform plan
```
