# Realtime Compute complete example

VPC-bound Flink VVP instance with `resource_spec` (cpu/memory) and OSS storage. Override `vpc_id`, `vswitch_ids`, `zone_id`, and `storage.oss_bucket` before apply. Optional `deployments` map can be filled after the workspace exists.

```bash
terraform init
terraform plan
```
