# Elasticsearch complete example

Enterprise production-oriented Elasticsearch: disk encryption, private whitelist (VPC CIDR), no public endpoint, Kibana private preferred, `data_node_amount >= 2`, PostPaid for examples.

Override `instances` (set real `vswitch_id` and `password`) before apply.

```bash
terraform init
terraform plan
```
